// Used on the public landing page only, where there's no logged-in farmer
// profile yet to pull a real state from. Change this to whatever state your
// uploaded market CSV actually covers — it's just a sane default, not tied
// to any real user.
export const DEFAULT_PUBLIC_STATE = "Telangana";